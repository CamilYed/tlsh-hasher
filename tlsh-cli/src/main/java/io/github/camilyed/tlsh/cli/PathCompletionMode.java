package io.github.camilyed.tlsh.cli;

/** Selects which filesystem entries should be suggested by an interactive path prompt. */
enum PathCompletionMode {
  FILES_AND_DIRECTORIES,
  DIRECTORIES_ONLY
}
